---
import BaseLayout from "./BaseLayout.astro";
const base = import.meta.env.BASE_URL;
const { title } = Astro.props;
---

<BaseLayout title={title}>
  <a href={`${base}blog`}>« Go back</a>
  <slot />
</BaseLayout>
